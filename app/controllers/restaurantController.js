// import { YELP_API_KEY } from '../config/env';

const Restaurants = {
  getRestaurants: (req, res) => {
    res.send('This was called!');
  },
  getSingleRestaurant: (req, res) => {
    res.send('This was called!');
  },
};

export default Restaurants;
